# Домашнее задание к занятию «Защита сети»

## Выполнил

**Масайлов Александр**

---

## Задание 1

На защищаемую систему были установлены Suricata и Fail2Ban. На атакующую систему были установлены Nmap и Hydra.

Suricata и Fail2Ban успешно установлены и запущены.

![](скрин1.png)

![](скрин2.png)

Были выполнены следующие команды:

```bash
sudo nmap -sA 10.129.0.16
sudo nmap -sT 10.129.0.16
sudo nmap -sS 10.129.0.16
sudo nmap -sV 10.129.0.16
```

В результате сканирования были обнаружены открытые службы:

- SSH (22)
- MySQL (3306)

Результаты сканирования:

![](скрин3.png)

![](скрин4.png)

![](скрин5.png)

![](скрин6.png)

Журнал работы Suricata:

![](скрин7.png)

Состояние Fail2Ban до атаки:

![](скрин8.png)

---

## Задание 2

Создал файлы `users.txt` и `pass.txt`, после чего выполнил подбор пароля командой:

```bash
hydra -L users.txt -P pass.txt 10.129.0.16 ssh -t 4
```

Hydra успешно подобрала пароль пользователя:

- пользователь: `user`
- пароль: `password`

Результат работы Hydra:

![](скрин9.png)

После нескольких неудачных попыток входа Fail2Ban автоматически заблокировал IP-адрес атакующей машины.

![](скрин10.png)

В журнале SSH видно множество неудачных попыток авторизации и работу защиты.

![](скрин11.png)

---

## Вывод

В ходе работы выполнил сканирование защищаемой системы с помощью Nmap и подбор пароля к SSH с помощью Hydra. Suricata контролировала сетевой трафик, а Fail2Ban обнаружил многочисленные неудачные попытки входа и автоматически заблокировал IP-адрес атакующей машины. Это показало, что Fail2Ban эффективно защищает SSH от атак методом перебора паролей.
